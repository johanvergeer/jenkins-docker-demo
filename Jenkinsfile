throttle(['throttleDocker']){
    node('docker') {
        wrap([Class: 'AnsiColorBuildWrapper']) {
            stage('Setup'){
                checkout scm
                echo "qwerty"
            }
        }
    }
}